import javafx.application.Application;
//this Class is the Main class and as soon as you run it it launches the VornoiApp by calling the VornoiWindow Class
public class Main {
    public static void main(String[] args) {
        Application.launch(VornoiWindow.class, args);//launches the VornoiApp
    }
}
